import { createTheme } from "@mui/material";

export const colors = {
  green: "#189E61",
  white: "#FFFFFF",
  primaryText: "#15284F",
  secondaryText: "#989696",
  iconContainer: "#F0EEEF",
};
export const theme = createTheme();
